name:                topocity
version:             0.1.0.0
github:              "anonymized/topocity"
license:             OtherLicense
author:              "Unknown"
maintainer:          "unknown@gmail.com"
copyright:           "2019 Unknown"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub.

dependencies:
- BiGUL
- hxt
- base >= 4.7 && < 5
- graphviz
- text
- citygml4hs
- fgl
- process
- turtle

library:
  source-dirs:
  - src

executables:
  topocity-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - topocity
    - structured-cli
    - data-default
    - mtl

tests:
  topocity-test:
    main:                Spec.hs
    source-dirs:
    - test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - topocity
    - tasty
    - tasty-hunit
    - tasty-smallcheck
    - tasty-quickcheck
    - tasty-hspec
    - turtle
